import { SimpleFeaturesDependencies } from '@node-in-layers/core/index.js'
import {
  {{appNameTitleCase}}ServicesLayer,
  {{appNameTitleCase}}FeaturesLayer
} from './types.js'


const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: SimpleFeaturesDependencies<{{appNameTitleCase}}ServicesLayer, {{appNameTitleCase}}FeaturesLayer>
) => {
  return {
  }
}

export {
  create,
}
