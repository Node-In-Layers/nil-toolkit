import { SimpleFeaturesDependencies } from '@node-in-layers/core/index.js'
import {
  {{packageNameTitleCase}}ServicesLayer,
  {{packageNameTitleCase}}FeaturesLayer
} from './types.js'


const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: SimpleFeaturesDependencies<{{packageNameTitleCase}}ServicesLayer, {{packageNameTitleCase}}FeaturesLayer>
) => {
  return {
  }
}

export {
  create,
}
