import { Config, FeaturesDependencies } from '@node-in-layers/core/index.js'
import {
  {{packageNameTitleCase}}ServicesLayer,
  {{packageNameTitleCase}}FeaturesLayer
} from './types.js'


const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: FeaturesDependencies<Config, {{packageNameTitleCase}}ServicesLayer, {{packageNameTitleCase}}FeaturesLayer>
) => {
  return {
  }
}

export {
  create,
}
