import { Namespace } from '../types.js'

const services = {
  create: () => {
    return {}
  },
}

const features = {
  create: () => {
    return {}
  },
}

const name = Namespace.system
export { services, features, name }
