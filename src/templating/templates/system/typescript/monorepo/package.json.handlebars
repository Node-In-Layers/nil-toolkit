{
  "name": "{{systemName}}",
  "private": true,
  "workspaces": [
    "{{sdkName}}",
    "{{backendName}}"{{#if frontendName}},
    "{{frontendName}}"{{/if}}
  ],
  "scripts": {
    "bootstrap": "npm install",
    "build": "npm --workspaces run build",
    "dev": "npm --workspaces run dev",
    "test": "npm --workspaces run test"
  }
}


