import { Config, FeaturesContext } from '@node-in-layers/core/index.js'
import {
  {{appNameTitleCase}}ServicesLayer,
  {{appNameTitleCase}}FeaturesLayer
} from './types.js'


const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: FeaturesContext<Config, {{appNameTitleCase}}ServicesLayer, {{appNameTitleCase}}FeaturesLayer>
) => {
  return {
  }
}

export {
  create,
}
