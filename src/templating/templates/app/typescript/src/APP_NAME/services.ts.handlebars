import { ServicesDependencies } from "@node-in-layers/core"

import { {{appNameTitleCase}}Services } from './types.js'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const create = (context: ServicesDependencies) : {{appNameTitleCase}}Services => {
  return {
  }
}

export {
  create,
}
