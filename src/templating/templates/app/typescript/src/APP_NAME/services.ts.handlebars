import { ServicesContext } from "@node-in-layers/core"

import { {{appNameTitleCase}}Services } from './types.js'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const create = (context: ServicesContext) : {{appNameTitleCase}}Services => {
  return {
  }
}

export {
  create,
}
