# {{systemName}} - a React Website written using Node in Layers

## Development Start
You can run the development server by running the following command:
```bash
npm run start
```

## Building for Production
You can build the react application for deployment by running the following command:
```bash
npm run build
```

## Shell
You can run the shell by running.
```bash
npm run shell -- dev
```

