// NOTE: JsonAble stuff ONLY.

import {
  CoreNamespace,
  LogFormat,
  LogLevelNames,
} from '@node-in-layers/core/index.js'
import type { LocalConfig } from './src/types.js'
import { McpClientNamespace } from '@node-in-layers/mcp-client'

export default (): LocalConfig => ({
  systemName: '{{systemName}}',
  environment: 'local',
  [CoreNamespace.root]: {
    // This is added later in the system.
    //domains: await Promise.all([import(`./home/index.js`)]),
    layerOrder: ['services', ['features', 'forms'], 'entries'],
    logging: {
      logLevel: LogLevelNames.warn,
      logFormat: LogFormat.json,
    },
  },
  useOAuth2: false,
  [McpClientNamespace.client]: {
    mcp: {
      stateless: true,
      connection: {
        type: 'http',
        url: 'http://localhost:3000',
      }
    }
  },
})
