# {{ basicName }}- Created by Node in Layers Toolkit

This is a basic Node In Layers executable package (generated by the Node in Layers Toolkit).

## Using the Shell
You can use the shell to interact with the system.

```bash
npm run shell local
```
