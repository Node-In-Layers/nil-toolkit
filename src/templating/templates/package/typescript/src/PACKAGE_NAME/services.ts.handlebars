import { ServicesContext } from "@node-in-layers/core/index.js"

import { {{packageNameTitleCase}}Services } from './types.js'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const create = (context: ServicesContext) : {{packageNameTitleCase}}Services => {
  return {
  }
}

export {
  create,
}
