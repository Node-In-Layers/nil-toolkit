import { Config, FeaturesContext } from '@node-in-layers/core/index.js'
import {
  {{packageNameTitleCase}}ServicesLayer,
  {{packageNameTitleCase}}FeaturesLayer
} from './types.js'


export const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: FeaturesContext<Config, {{packageNameTitleCase}}ServicesLayer, {{packageNameTitleCase}}FeaturesLayer>
) => {
  return {
  }
}
