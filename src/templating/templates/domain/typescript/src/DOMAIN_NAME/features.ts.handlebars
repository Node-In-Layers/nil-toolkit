import { Config, FeaturesContext } from '@node-in-layers/core'
import {
  {{domainNameTitleCase}}ServicesLayer,
  {{domainNameTitleCase}}FeaturesLayer
} from './types.js'


export const create = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  context: FeaturesContext<Config, {{domainNameTitleCase}}ServicesLayer, {{domainNameTitleCase}}FeaturesLayer>
) => {
  return {
  }
}


